#
# helix configuration for local development
#
version: 1

definitions:
  defaults:
    - &defaultRepo "$CURRENT_ORIGIN"

strains:
  - name: default
    code: *defaultRepo
    content: *defaultRepo
    static: *defaultRepo
